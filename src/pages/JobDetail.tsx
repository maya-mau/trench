import React from 'react'
import { useParams } from "react-router-dom";

const jobPage = () => {
    return (
        <div className="text-black">This is a job page</div>
    );
};

export default jobPage;